import Page from "../../components/page/Page";
import { LoginPageProperties } from "./LoginPageProperties";

export default function LoginPage(properties: LoginPageProperties): React.JSX.Element {

    //Sign up
        //for now only email + password
    //Sign in
        //Google account (Poup, not redirect)
        //email + password
        //demo account setup via email + password

    console.log(properties);

    return (
        <Page documentTitle="WebChat: Login">
            
        </Page>
    )
}